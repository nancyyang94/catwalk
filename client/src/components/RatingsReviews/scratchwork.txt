  const [characteristic, setCharacteristic] = useState('nothing');
  const [value, setValue] = useState(0);

  const factors = getReviewsMeta(id).then((response) => {
    return response.data.characteristics;
  }).catch((error) => {
    console.log(error);
  })

  factors.then((response) => {
    for (var key in response) {
      setCharacteristic(characteristic: key);
      setValue(value: response[key].value);
    }
  })