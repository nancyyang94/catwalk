import styled from 'styled-components';

const OnSaleText = styled.h1`
  font-size: 18px;
  line-height: 24px;
  font-family: Helvetica, Arial, sans-serif;
  font-weight: 700;
  letter-spacing: 1.5px;
  color: red;
  text-decoration: line-through;
  padding-right: 5px;

`;

export default OnSaleText;
