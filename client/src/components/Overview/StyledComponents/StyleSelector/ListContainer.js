import styled from 'styled-components';

const ListContainer = styled.div`
  height: 150px;
  display: flex;
  flex-wrap: wrap;
  padding-right: 20%;
  overflow: scroll;
`;

export default ListContainer;
