import styled from 'styled-components';

const SvgArrowL = styled.svg`
  transform: scale(0.3) rotate(-180deg);
  position: relative;
  z-index: -1;
  top: -14%;
  left: -30%;
`;

export default SvgArrowL;
