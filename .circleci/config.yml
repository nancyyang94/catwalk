version: '2.1'
orbs:
  node: circleci/node@2.0.2
jobs:
  test:
    docker:
      - image: 'cimg/base:stable'
    working_directory: ~/catwalk
    steps:
      - checkout
      - run: npx test
workflows:
  test_my_app:
    jobs:
      - test